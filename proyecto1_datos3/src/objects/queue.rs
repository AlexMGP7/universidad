use std::collections::VecDeque;
use super::cart::Carrito;

#[allow(dead_code)]
pub struct Cola{
    pub cola:VecDeque<Carrito>,
}

/* impl Cola {
    #[allow(dead_code)]
    pub fn cola_nueva() -> Self{
    
    } */